##Проста обробка текстових даних засобами оболонки Unix-подібних ОС інтепретатора команд ОС.

# Звіт до лабораторної роботи №3
**Тема:** Оболонка Bash та текстові фільтри
**Виконав:** Дорошков Родіон Костянтинович

## Виконання роботи

### 1. Налаштування псевдонімів (Aliases)
Було створено та протестовано 5 псевдонімів:
* `зібрати` (mkdir), `встановити` (touch), `зняти` (rm), `простукати` (less), `дата_зібрати` (date).

### 2. Робота з потоками та конвеєрами
* Створено файли `Doroshkov_1` та `Rodion_2`.
* Виконано об'єднання файлів (`cat`, `paste`).
* Проаналізовано системну змінну `$PATH`: знайдено 23 каталоги.

## Висновок
Під час роботи я навчився працювати з редактором nano, створювати alias, перенаправляти потоки даних та будувати конвеєри (pipelines).
